{
  "tasks": {
    "fmt": {
      "description": "Check TypeScript formatting in this extension folder.",
      "command": "deno fmt --check *.ts"
    },
    "doc": {
      "description": "Generate documentation for the TypeScript sources in this extension folder.",
      "command": "deno doc *.ts"
    },
    "check": {
      "description": "Type check a folder of TypeScript sources with deno check.",
      "command": "deno check *.ts"
    },
    "lint": {
      "description": "Lint the TypeScript files in this extension folder.",
      "command": "deno lint *.ts"
    },
    "test": {
      "description": "Run unit tests for a TypeScript extension with required permissions.",
      "command": "deno test --allow-read --allow-write --allow-env=PATH --allow-run"
    },
    "swampfmt": {
      "description": "Format extension before pushing for swamp.",
      "command": "swamp extension fmt manifest.yaml --check"
    },
    "swampquality": {
      "description": "Quality check extension before push.",
      "command": "swamp extension quality manifest.yaml"
    },
    "swamppushdryrun": {
      "description": "Dry-run of publishing a swamp extension.",
      "command": "swamp extension push manifest.yaml --dry-run --yes"
    }
  },
  "lint": {
    "rules": {
      "exclude": [
        "no-import-prefix",
        "require-await"
      ]
    }
  }
}
